import React from 'react';
import { FormLogin,  Logo } from '../components';

const Auth = () => {
    return (
        <div>
            <Logo/>
            <FormLogin/>
        </div>
    );
};

export default Auth;