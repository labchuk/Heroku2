import React from 'react';
import style from "./Paginator.module.scss";

const Paginator = () => {
    return (
        <div>
            Plaginator
        </div>
    );
};

export default Paginator;