import React from 'react';
import { Graph, GraphPanel } from '../index';

const GraphInfo = () => {
    return (
        <div>
            GraphInfo
            <Graph/>
            <GraphPanel/>
        </div>
    );
};

export default GraphInfo;